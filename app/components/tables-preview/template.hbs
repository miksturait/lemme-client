{{#if showAddTableForm}}
  {{input type='number' value=seatsCount}}
  <button {{action 'addTable'}}>Add</button>
{{else}}
  <button {{action 'showForm'}}>Add table</button>
{{/if}}

{{#each tables as |table|}}
  {{tables-preview/table table=table}}
{{/each}}